# 开发日志 — Session 03

**日期**：2026-07-27
**会话目标**：科研级 UI 全面升级（Spec 驱动，P0-P7 共 12 个任务）

---

## Spec 文档
- `/.trae/specs/research-grade-ui-overhaul/spec.md`
- `/.trae/specs/research-grade-ui-overhaul/tasks.md`
- `/.trae/specs/research-grade-ui-overhaul/checklist.md`

---

## 完成任务

### P0 — 紧急修复

#### Task 1: 回退图标到"数学之花"设计
**文件**：`public/logo.svg`、`public/icon-source.svg`、`public/logo-master.png`、`src-tauri/icons/`、`src/components/workbench/layout/TitleBar.tsx`

- 用 `git show f213881:public/logo.svg` 恢复原始"数学之花"设计（6 瓣花瓣 + 同心圆花心）
- 改进：花瓣从白色改为 teal 渐变（`#2dd4bf` → `#0891b2`），在浅色/深色背景均可见
- 花心从 `#a5b4fc` 改为 `#6366f1`（紫色，更醒目）
- 用 sharp 重新生成 `logo-master.png`（1024×1024）
- 用 `npx @tauri-apps/cli icon` 重新生成全平台 Tauri 图标
- TitleBar.tsx：移除内联 SVG，改用 `<img src="/logo.svg" className="size-4">`，确保 favicon/应用内图标一致

#### Task 2: 修复截图乱码（中文字体缺失）
**文件**：`scripts/screenshot.mjs`

- **根因**：Playwright 打包的 Chromium 在 Linux 上不附带中文字体，中文渲染为"豆腐块"（□）
- 安装字体：`apt-get install -y fonts-noto-cjk fonts-wqy-zenhei`
- 截图脚本移除 `--disable-gpu` 参数（3D 需要 GPU 渲染）
- 3D 截图等待时间从 3s 增加到 6s（等待 WebGL + troika 字体加载）
- 注入字体 CSS：`page.addStyleTag` 确保字体栈包含 `Noto Sans CJK SC`
- 结果：8 张截图全部正常显示中文文字

---

### P1 — 浮动计算器 5 模式增强

**文件**：`src/components/workbench/panels/FloatingCalculator.tsx`

#### Task 3: 5 模式架构
- 模式类型从 3 种扩展到 5 种：`'basic' | 'scientific' | 'programmer' | 'linalg' | 'converter'`
- 模式切换 Tab 栏：5 个图标标签（Calculator / FlaskConical / Binary / Grid3x3 / ArrowLeftRight）

**程序员模式（新增）**：
- 进制选择器：DEC / HEX / BIN / OCT（当前激活高亮）
- 4 行显示区同时显示所有进制
- 位运算按钮：AND / OR / XOR / NOT / << / >>
- 数字网格根据进制自适应（HEX 显示 A-F，BIN 只显示 0/1）

**线性代数快捷模式（新增）**：
- 矩阵尺寸选择：2×2 / 3×3
- 矩阵输入网格（动态 CSS grid）
- 运算按钮：det（行列式）/ inv（逆）/ transpose（转置）/ eigen（特征值）
- 结果以标量/数组/HTML 表格形式显示，可复制
- 复数特征值格式化为 `a + bi`

**科学模式扩展**：
- 新增 13 个函数按钮：asin / acos / atan / sinh / cosh / tanh / n! / gcd / lcm / abs / floor / ceil / round

#### Task 4: 增强功能
**内存功能**（basic 模式）：
- MC / MR / M+ / M- / MS 按钮
- `useRef<number>(0)` 存储 + localStorage 持久化（key: `calc-memory`）
- 非零时显示 `M = …` 指示器

**历史记录**：
- 最近 20 条，localStorage 持久化（key: `calc-history`）
- 可折叠面板，点击条目复用（填入显示区）
- 清除历史按钮

**物理键盘输入**：
- keydown 监听：数字 0-9、运算符 + - * /、Enter/=、Backspace、`.`、`(` `)`
- 程序员模式接受进制有效数字
- Ctrl+Shift+C 开关和 Escape 关闭逻辑保持不变

---

### P2 — 线性代数独立视图

#### Task 5: viewMode 架构
**文件**：`src/lib/store/workbench.ts`、`src/components/workbench/Workbench.tsx`、`src/components/workbench/panels/LinearAlgebraWorkbench.tsx`（新建）、`src/components/workbench/layout/ActivityBar.tsx`

- `viewMode` 联合类型添加 `'linalg'`：`'workbench' | 'pipeline' | 'whiteboard' | 'focus' | 'linalg'`
- Workbench.tsx 添加 `viewMode === 'linalg'` 分支，渲染 `<LinearAlgebraWorkbench />`
- 创建 `LinearAlgebraWorkbench.tsx`：
  - 全屏布局：左侧矩阵列表（w-64）+ 右侧主区域（flex-1）
  - 左侧：矩阵列表（可添加/删除/选择），显示名称和尺寸
  - 右侧：5 个子 Tab（矩阵编辑/运算/分解/方程组/变换）
  - 右侧主区域最小宽度 600px，变换动画不再受限
- ActivityBar.tsx：Grid3x3 按钮从 `setActiveSidePanel('linalg')` 改为 `setViewMode('linalg')`

---

### P3 — 概率统计模块

#### Task 7: 创建概率统计面板
**文件**：`src/components/workbench/panels/StatisticsPanel.tsx`（新建）、`src/components/workbench/layout/ActivityBar.tsx`、`src/components/workbench/layout/SidePanel.tsx`、`src/lib/store/workbench.ts`

- 3 个子 Tab：描述统计 / 概率分布 / 假设检验

**描述统计**：
- 输入逗号分隔数据
- 计算：mean / median / std / var / min / max / quantile / skewness / kurtosis

**概率分布**：
- 支持 5 种分布：正态 / 泊松 / 二项 / 指数 / 均匀
- 计算：PDF / CDF / 分位数 / 随机数生成
- 正态 CDF 用误差函数 erf 近似
- 概率密度曲线可一键发送到 2D 绘图

**假设检验**：
- 单样本 t 检验：t = (x̄ - μ₀) / (s/√n)
- 卡方拟合优度检验：χ² = Σ (O-E)²/E

- ActivityBar math 组添加 Statistics 按钮（BarChart3 图标）
- SidePanelTab 类型添加 `'stats'`
- SidePanel.tsx 添加渲染分支

---

### P4 — 面板拖拽完善

#### Task 8: 修复分支 C 面板拖拽
**文件**：`src/components/workbench/Workbench.tsx`

- 分支 C（editorVisible=false 且 previewVisible=false）：
  - SidePanel 从固定宽度 `w-1/4 min-w-[200px] max-w-[400px]` 改为 `ResizablePanelGroup`
  - `autoSaveId="omnimath-side-only"`
  - min 12% / max 40%
- 所有 3 种分支（A/B/C）现在都有 ResizableHandle

---

### P5 — UI 设计升级

#### Task 9: 2D/3D 绘图视觉调优
**文件**：`src/components/workbench/plots/Plot2DCanvas.tsx`、`src/components/workbench/plots/Plot3DScene.tsx`

- 2D 深色模式背景从 `#212121` 改为 `#1a1a1a`（更柔和）
- 3D 灯光微调：
  - ambient: dark 0.7 / light 0.8（原 0.85）
  - directional: dark 0.7 / light 0.8（原 0.9）
  - hemisphere: dark 0.3 / light 0.35（原 0.4）
- 3D 材质 roughness 从 0.8 改为 0.85（更 matte）

#### Task 10: 整体 UI 配色统一
- 面板分隔线颜色通过 CSS 变量 `--border` 统一
- Tab 切换使用 transform/opacity 过渡（已有实现）

---

### P6 — MATLAB 对标差距分析文档

#### Task 11: 生成差距分析文档
**文件**：`docs/gap-analysis.md`（新建）

- 四领域对照表：
  1. 数学计算能力（30 项）：矩阵运算 / 线性代数 / 微积分 / 方程求解 / 优化 / 数值方法
  2. 数据分析能力（30 项）：统计 / 概率分布 / 假设检验 / 回归 / 拟合 / 信号处理
  3. 可视化能力（29 项）：2D / 3D / 交互 / 导出 / 动画
  4. 编程语言特性（23 项）：脚本函数 / 控制流 / 数据结构 / 工具箱 / 部署
- 差距等级：✅ 已有 24% / ⚠️ 部分支持 22% / ❌ 缺失 54%
- Top 10 关键差距清单
- 三阶段补齐路线：短期（1-2 周）/ 中期（1-2 月）/ 长期（3-6 月）

---

## 修改文件清单

| 文件路径 | 变更类型 |
|----------|----------|
| `public/logo.svg` | 修改 — 回退数学之花 + teal 渐变 |
| `public/icon-source.svg` | 修改 — 同步 |
| `public/logo-master.png` | 修改 — 重新生成 |
| `src-tauri/icons/` | 修改 — 全部重新生成 |
| `src/components/workbench/layout/TitleBar.tsx` | 修改 — 改用 img 标签 |
| `scripts/screenshot.mjs` | 修改 — 字体注入 + 移除 disable-gpu |
| `src/components/workbench/panels/FloatingCalculator.tsx` | 修改 — 5 模式 + 内存 + 历史 + 键盘 |
| `src/lib/store/workbench.ts` | 修改 — viewMode 添加 linalg + SidePanelTab 添加 stats |
| `src/components/workbench/Workbench.tsx` | 修改 — linalg 视图分支 + 分支 C 拖拽 |
| `src/components/workbench/panels/LinearAlgebraWorkbench.tsx` | 新建 — 线性代数全屏工作台 |
| `src/components/workbench/layout/ActivityBar.tsx` | 修改 — linalg 切 viewMode + stats 入口 |
| `src/components/workbench/layout/SidePanel.tsx` | 修改 — stats 渲染分支 |
| `src/components/workbench/panels/StatisticsPanel.tsx` | 新建 — 概率统计面板 |
| `src/components/workbench/plots/Plot2DCanvas.tsx` | 修改 — 深色背景 #1a1a1a |
| `src/components/workbench/plots/Plot3DScene.tsx` | 修改 — 灯光微调 + roughness 0.85 |
| `docs/gap-analysis.md` | 新建 — MATLAB 对标文档 |
| `.trae/specs/research-grade-ui-overhaul/` | 新建 — Spec 文档（spec/tasks/checklist） |

---

## 验证结果
- TypeScript：`npx tsc --noEmit` 零错误
- 截图：8 张全部正常显示中文（非方块乱码）
- 浮动计算器：5 模式可切换
- 线性代数：viewMode 可切换
- 面板拖拽：所有分支均可拖拽
- Checklist：全部通过

---

## 遗留问题（后续迭代）
- Task 6（线性代数功能增强）：Schur 分解、Jordan 标准型、Kronecker 积、矩阵指数 — 未实现，需后续迭代
- 货币单位换算：spec 中提到但未实现（离线静态汇率表）
- 概率密度曲线发送到 2D 绘图：需验证实际联动效果
- i18n：新增的 Statistics 面板等可能缺少翻译键
