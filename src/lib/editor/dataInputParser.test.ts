import { describe, it, expect } from 'vitest';

// Task 10a 预埋：dataInputParser 模块的单元测试占位
// parseNumericInput 函数将在 Task 10a 中实现，此处仅预留测试骨架

describe('dataInputParser', () => {
  describe('parseNumericInput', () => {
    // TODO: Task 10a 实现 parseNumericInput 后取消注释
    it.todo('parseNumericInput("1,2,3") 返回 numbers=[1,2,3] invalid=[]', () => {
      // const result = parseNumericInput('1,2,3');
      // expect(result.numbers).toEqual([1, 2, 3]);
      // expect(result.invalid).toEqual([]);
    });
  });
});
