# OmniMath Pro — 架构重构与发展路线（终版）

> **本文档基于**：
> 1. **2026-07-24 完整代码审查**（实际读取 15 个核心文件，定位 15 个架构问题，含行号）
> 2. **上一版发展路线 MD**（《OmniMath-Pro-下一步发展路线.md》）
> 3. **GitHub 开源资源调研**（JSXGraph / react-math-viz / geometry-node / mathjs 生态）
> 4. **用户核心诉求**：白板统一交互、滑块→图像→曲线反向、2D 比例乱、3D 没用、蓝图浅
>
> **核心主张**：不要继续堆功能。先用**三层架构**收敛现有混乱，再分层升级。

---

## 目录

- [一、代码审查：15 个真实架构问题（含行号）](#一代码审查15-个真实架构问题含行号)
- [二、核心架构主张：三层架构](#二核心架构主张三层架构)
- [三、2D 重建：解决"又杂又乱、比例不一样"](#三2d-重建解决又杂又乱比例不一样)
- [四、3D 重建：解决"只看个图、没用"](#四3d-重建解决只看个图没用)
- [五、蓝图重建：解决"只会加减乘除"](#五蓝图重建解决只会加减乘除)
- [六、白板：统一交互层（核心差异化）](#六白板统一交互层核心差异化)
- [七、图像→曲线反向功能（用户创意）](#七图像曲线反向功能用户创意)
- [八、GitHub 资源整合方案](#八github-资源整合方案)
- [九、收敛的执行路线](#九收敛的执行路线)

---

## 一、代码审查：15 个真实架构问题（含行号）

> 以下问题均来自实际读取代码，非推测。这是"为什么很烂"的根因清单。

### 1.1 引擎层（最严重）

| # | 问题 | 位置 | 后果 |
|---|---|---|---|
| **E1** | **4 个独立 mathjs 实例，scope 不共享** | [evaluator.ts:46](file:///workspace/src/lib/engine/evaluator.ts#L46) + [plot2d.ts:21](file:///workspace/src/lib/plots/plot2d.ts#L21) + [plot3d.ts:20](file:///workspace/src/lib/plots/plot3d.ts#L20) + [pipelineEngine.ts:14](file:///workspace/src/components/workbench/nodes/pipelineEngine.ts#L14) | 用户在 editor 写 `a=5`，plot 表达式 `sin(a*x)` 报"Undefined symbol a" |
| **E2** | **log/ln 重写不统一** | [evaluator.ts:57-81](file:///workspace/src/lib/engine/evaluator.ts#L57-L81) 重写了，其他 3 个实例没重写 | `log(100)` 在 editor 返回 2，在 plot/蓝图返回 4.605，**行为不一致** |
| **E3** | **没有 mathInstance.ts 单例** | 应有但不存在 | 设计想做单例未落地，4 份 `create(all)` 各占内存 |

### 1.2 2D 绘图层

| # | 问题 | 位置 | 后果 |
|---|---|---|---|
| **D1** | **多 plot Y 轴用中位数聚合** | [Plot2DPanel.tsx:39-60](file:///workspace/src/components/workbench/panels/Plot2DPanel.tsx#L39-L60) `deriveDefaultY` + [:53-59](file:///workspace/src/components/workbench/panels/Plot2DPanel.tsx#L53-L59) | **这就是"sin x 和 e^x 比例不一样"的根因**：sin 范围 [-1,1]，e^x 范围 [-0.1, 22000]，中位数聚合后 sin 变成贴着 y=0 的直线 |
| **D2** | **滑块改值不触发 plot 重算** | [VariablesPanel.tsx:170-173](file:///workspace/src/components/workbench/panels/VariablesPanel.tsx#L170-L173) 写 scope，但 [Plot2DCanvas.tsx:158-180](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L158-L180) `computed` 不读 variables | **这就是"没有滑块"的根因**：有滑块但断了链 |
| **D3** | **每帧重设 canvas.width/height** | [Plot2DCanvas.tsx:217-223](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L217-L223) | 悬停时 60fps 重分配 GPU backing storage，卡顿 |
| **D4** | **全量重绘无分层** | [Plot2DCanvas.tsx:209-549](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L209-L549) `drawNow` | 网格/曲线/标注/十字线每次全画，无 offscreenCanvas 缓存 |
| **D5** | **wheel preventDefault 失效** | [Plot2DCanvas.tsx:595-619](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L595-L619) | React onWheel 是 passive，页面跟随滚动 |
| **D6** | **cartesian snap break 条件错误** | [Plot2DCanvas.tsx:686](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L686) | `wx + range` 应为 `viewRef.current.x[1]`，导致右半区 snap 失效 |
| **D7** | **无导数标注** | [plot2d.ts:266-299](file:///workspace/src/lib/plots/plot2d.ts#L266-L299) `findExtrema` 只检测极值/零点 | 用户说"求导显示麻烦"——实际是根本没渲染到画布 |

### 1.3 3D 绘图层

| # | 问题 | 位置 | 后果 |
|---|---|---|---|
| **T1** | **仅支持 z=f(x,y) 曲面** | [plot3d.ts:48-58](file:///workspace/src/lib/plots/plot3d.ts#L48-L58) `Surface3DData` + [:138](file:///workspace/src/lib/plots/plot3d.ts#L138) `sampleSurface` | 无 cube/sphere/cone/参数曲面，**这就是"只看个图"的根因** |
| **T2** | **采样阻塞主线程** | [plot3d.ts:162-188](file:///workspace/src/lib/plots/plot3d.ts#L162-L188) N=100 时 10000 次 mathjs.evaluate | 5 个曲面 = 10s 卡死 |
| **T3** | **WebGL 上下文泄漏** | [PreviewPanel.tsx:168-188](file:///workspace/src/components/workbench/layout/PreviewPanel.tsx#L168-L188) + [Plot3DPanel.tsx:581-590](file:///workspace/src/components/workbench/plots/Plot3DPanel.tsx#L581-L590) 放大 dialog 双 Canvas | 切 16 次黑屏 |
| **T4** | **preserveDrawingBuffer 常驻 true** | [Plot3DScene.tsx:478](file:///workspace/src/components/workbench/plots/Plot3DScene.tsx#L478) | 性能损失 10-30% |
| **T5** | **轴长度不考虑 Z 范围** | [Plot3DScene.tsx:379-393](file:///workspace/src/components/workbench/plots/Plot3DScene.tsx#L379-L393) | 高曲面溢出轴框 |

### 1.4 蓝图层

| # | 问题 | 位置 | 后果 |
|---|---|---|---|
| **P1** | **nodes/edges 在组件 local state** | [NodePipeline.tsx:237-238](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L237-L238) | AI/命令面板无法访问蓝图 |
| **P2** | **Run 后不切预览 tab** | [NodePipeline.tsx:353-395](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L353-L395) `addPlot` 后无 `setActivePreviewTab` | "改图后节点数字变，但绘图不进预览" |
| **P3** | **无撤销/重做/多选/右键** | [NodePipeline.tsx:239](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L239) 仅 `selectedId: string\|null` | "反人类" |
| **P4** | **环检测是事后** | [pipelineEngine.ts:632-640](file:///workspace/src/components/workbench/nodes/pipelineEngine.ts#L632-L640) Kahn 排序后才标记 | 连边时不预防 |
| **P5** | **节点仅 14 种且浅** | [pipelineEngine.ts:188-511](file:///workspace/src/components/workbench/nodes/pipelineEngine.ts#L188-L511) | "加减乘除玩具" |
| **P6** | **单击边即删除** | [NodePipeline.tsx:1719](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L1719) pointerup 触发 onDelete | 误删痛苦 |

---

## 二、核心架构主张：三层架构

> 用户说"全部定义起来感觉太乱，按你的想法来"。我的主张是：**不要继续堆功能，先用三层架构收敛**。

```
┌─────────────────────────────────────────────────────────┐
│  白板层 (Whiteboard) ── 统一交互入口                      │
│  独立模式 / 节点模式 / 2D触发 / 3D触发                    │
└────────────────────────┬────────────────────────────────┘
                         │ 用户意图（手绘/草图/图像）
                         ▼
┌─────────────────────────────────────────────────────────┐
│  蓝图层 (Pipeline) ── 编排引擎                           │
│  节点生态 / 拓扑执行 / 状态全局化 / 撤销重做              │
└────────────────────────┬────────────────────────────────┘
                         │ 计算任务（表达式/矩阵/几何体）
                         ▼
┌─────────────────────────────────────────────────────────┐
│  引擎层 (Engine) ── 统一计算                              │
│  mathjs 单例 / 共享 scope / Web Worker / 符号计算        │
└─────────────────────────────────────────────────────────┘
```

### 2.1 为什么是三层

| 层 | 职责 | 现状问题 | 重构目标 |
|---|---|---|---|
| **白板** | 用户意图入口（手绘/草图/图像/滑块） | 无白板，输入只有文本 | 统一所有"输入方式"到白板 |
| **蓝图** | 编排计算流程 | local state、14 节点、无撤销 | 全局 store、40+ 节点、专业交互 |
| **引擎** | 执行数学计算 | 4 个 mathjs 实例 scope 割裂 | 单例 + 共享 scope + Worker |

### 2.2 三层的数据流

```
用户在白板手绘 → 白板识别为 {type:'function', latex:'y=x²'} 
   ↓ (作为 sketch-input 节点输出)
蓝图接收 expression → 连到 derivative 节点 → 连到 plot-output 节点
   ↓ (蓝图调度)
引擎统一 mathjs 单例求导 + 采样 → 返回 plot 数据
   ↓ (引擎返回)
蓝图 plot-output 节点 → 写入 workbench.plots → 预览面板渲染
```

**关键**：白板和蓝图不是平行功能，是**白板喂给蓝图，蓝图调度引擎**。

---

## 三、2D 重建：解决"又杂又乱、比例不一样"

### 3.1 根因定位（用户原话对应代码）

| 用户抱怨 | 根因代码 | 修复方向 |
|---|---|---|
| "sin x 和 e^x 比例都不一样" | [Plot2DPanel.tsx:53-59](file:///workspace/src/components/workbench/panels/Plot2DPanel.tsx#L53-L59) 中位数聚合 Y 轴 | 双 Y 轴 + 归一化 |
| "比较两个图像麻烦" | 无独立图层管理、无对齐工具 | 图层面板 + 对齐网格 |
| "极值点求导显示麻烦" | [plot2d.ts:266-299](file:///workspace/src/lib/plots/plot2d.ts#L266-L299) 只检测极值，导数不渲染 | 分层标注 + 导数叠加 |
| "没有滑块" | [Plot2DCanvas.tsx:158-180](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L158-L180) 不读 variables | 响应式重算链路 |

### 3.2 修复 1：Y 轴双轴 + 归一化（解决比例问题）

**现状**：所有 plot 共用一个 Y 轴，中位数聚合。

**方案**：引入"主轴/副轴"+ "归一化模式"切换：

```typescript
// Plot2DPanel.tsx 新增 Y 轴模式
type YAxisMode = 'shared' | 'dual' | 'normalized';

// shared: 现状（中位数聚合）—— 适合同量级
// dual: 左轴 plot[0]，右轴 plot[1]—— 适合 sin vs e^x
// normalized: 每个 plot 归一化到 [0,1]—— 适合形状对比
```

**实现**：
- `Plot2DCanvas` 增加右侧 Y 轴绘制（副轴）
- 归一化模式：每个 plot 的 y 按 `(y - min) / (max - min)` 映射
- 图例 chip 上加轴标识（L1/L2/N）

### 3.3 修复 2：响应式重算链路（解决"没有滑块"）

**断点**：[VariablesPanel.tsx:170-173](file:///workspace/src/components/workbench/panels/VariablesPanel.tsx#L170-L173) 写 scope，但 [Plot2DCanvas.tsx:158-180](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L158-L180) 不读 variables。

**修复**：让 `computed` 依赖 `variables`：

```typescript
// Plot2DCanvas.tsx 改造
const variables = useWorkbenchStore(s => s.variables);
const computed = useMemo(() => {
  return plots.map(p => {
    // 把 variables 注入采样 scope
    const scope = { ...variables };  // 关键：注入
    return sampleFunction(p.expression, p.xRange, p.plotType, 300, scope);
  });
}, [plots, variables]);  // 关键：依赖 variables
```

**配套**：VariablesPanel 滑块 `onValueChange` → `setVariable` → 触发 `computed` 重算 → 画布刷新。

### 3.4 修复 3：表达式扫描器（Desmos 范式 1）

**新增**：`src/lib/engine/variableScanner.ts`

```typescript
import { parse } from 'mathjs';

export function scanFreeVariables(
  expressions: string[],
  definedVars: Record<string, unknown>
): string[] {
  const free = new Set<string>();
  const reserved = new Set(['x','y','t','u','v','pi','e','i']);
  for (const expr of expressions) {
    try {
      parse(expr).traverse((n: any) => {
        if (n.type === 'SymbolNode' && 
            !(n.name in definedVars) && !reserved.has(n.name)) {
          free.add(n.name);
        }
      });
    } catch {}
  }
  return Array.from(free);
}
```

**接入**：EditorPanel `onChange` → 扫描 → 自动在 VariablesPanel 注册 + 弹滑块。

### 3.5 修复 4：分层 Canvas（解决"显示麻烦"）

**现状**：[Plot2DCanvas.tsx:209-549](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L209-L549) 全量重绘。

**方案**：4 层 offscreenCanvas：

| 层 | 内容 | 更新时机 |
|---|---|---|
| L1 静态层 | 网格、坐标轴、刻度 | 仅 view 变化时 |
| L2 曲线层 | 函数曲线 | 仅 plots/variables 变化时 |
| L3 标注层 | 极值/零点/导数/交点 | 仅 computed 变化时 |
| L4 交互层 | 十字线、悬停点、拖拽点 | 每帧（仅 clearRect + draw） |

**收益**：悬停时只重绘 L4，性能提升 10x。

### 3.6 修复 5：补全标注类型

**现状**：[plot2d.ts:266-299](file:///workspace/src/lib/plots/plot2d.ts#L266-L299) `findExtrema` 只检测极值/零点。

**扩展**：
- **导数叠加**：`f(x)` 和 `f'(x)` 同图，虚线区分
- **积分面积**：`fillBetween(f, a, b)` 填充
- **切线**：点曲线上点 → 自动画切线
- **交点**：两曲线交点画圆点 + 标坐标
- **拐点**：二阶导零点

### 3.7 修复 6：表达式图层管理（解决"比较麻烦"）

**新增**：图层面板（类似 Photoshop 图层）

```
┌─────────────────────────────┐
│ 图层                    [+] │
├─────────────────────────────┤
│ 👁 🔒 📌 sin(x)      L1 ●teal│
│ 👁 🔒 📌 e^x        L2 ●amber│
│ 👁 🔒 📌 f'(x)      L3 ●rose │
└─────────────────────────────┘
   可见 锁定 置顶  表达式  轴  颜色
```

- **可见**：`visible`（已有）
- **锁定**：防止误改
- **置顶**：绘制顺序
- **轴**：L1/L2 归属（双轴）
- **颜色**：可改

### 3.8 修复 7：修复具体 Bug

| Bug | 位置 | 修复 |
|---|---|---|
| 每帧重设画布 | [Plot2DCanvas.tsx:217-223](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L217-L223) | 仅 ResizeObserver 回调里设 |
| wheel passive | [Plot2DCanvas.tsx:595-619](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L595-L619) | `addEventListener('wheel', fn, {passive:false})` |
| snap break 错误 | [Plot2DCanvas.tsx:686](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx#L686) | `wx + range` → `viewRef.current.x[1]` |
| autoYRange 无离群点 | [plot2d.ts:112-130](file:///workspace/src/lib/plots/plot2d.ts#L112-L130) | IQR 检测 |

---

## 四、3D 重建：解决"只看个图、没用"

### 4.1 根因定位

| 用户抱怨 | 根因代码 | 修复方向 |
|---|---|---|
| "就看到一个图像" | [plot3d.ts:48-58](file:///workspace/src/lib/plots/plot3d.ts#L48-L58) 仅 z=f(x,y) | 几何体引擎 |
| "啥都看不了" | 无截面/无展开/无标注 | 3D 工具集 |
| "切几次就崩" | [PreviewPanel.tsx:168-188](file:///workspace/src/components/workbench/layout/PreviewPanel.tsx#L168-L188) WebGL 泄漏 | 上下文池化 |
| "拖一下就卡" | [plot3d.ts:162-188](file:///workspace/src/lib/plots/plot3d.ts#L162-L188) 同步阻塞 | Web Worker |

### 4.2 修复 1：3D 几何体引擎

**新增类型**（[types.ts](file:///workspace/src/lib/engine/types.ts)）：

```typescript
export type GeometryShape =
  | { kind: 'cube'; side: number }
  | { kind: 'cuboid'; w: number; h: number; d: number }
  | { kind: 'sphere'; radius: number }
  | { kind: 'cylinder'; radius: number; height: number }
  | { kind: 'cone'; radius: number; height: number }
  | { kind: 'prism'; sides: number; radius: number; height: number }
  | { kind: 'pyramid'; sides: number; radius: number; height: number }
  | { kind: 'torus'; major: number; minor: number };
```

**动词**：`cube(3)` / `sphere(r=5)` / `cone(r=3,h=8)` / ...

**渲染**：复用 [Plot3DScene.tsx](file:///workspace/src/components/workbench/plots/Plot3DScene.tsx) 的 `<Canvas>`，新增 `GeometryMesh` 组件用 three.js 内置 `BoxGeometry`/`SphereGeometry`/`ConeGeometry`。

### 4.3 修复 2：3D 工具集（对标 GeoGebra + AI 黑板）

| 工具 | 实现 | 对标 |
|---|---|---|
| **截面** | three-bvh-csg 平面切几何体 | GeoGebra `Intersect` |
| **2D 截面视图** | 截面 polygon → 独立 2D canvas | GeoGebra "Create 2D view" |
| **可拖平面** | 3 点定义平面，滑块调角度 | Desmos 3D 滑块切圆锥 |
| **展开图** | 预计算展开 mesh，动画展开 | AI 黑板 |
| **顶点/棱/面标注** | troika-three-text | AI 黑板 |
| **属性计算** | mathjs 算表面积/体积/棱长 | AI 黑板 |
| **填充/透明** | material.opacity | AI 黑板 |

### 4.4 修复 3：滑块切圆锥曲线（Desmos 3D 经典）

```
平面: z = tan(θ) * y + b   (θ/b 是滑块)
几何体: cone(r=3, h=5)
截面: intersect(cone, plane)

θ 从 0→π/2:
  0      → 圆
  0~π/4  → 椭圆
  π/4    → 抛物线
  >π/4   → 双曲线
```

做了这个 = Desmos 3D 级别。

### 4.5 修复 4：WebGL 上下文池化

**现状**：[PreviewPanel.tsx:168-188](file:///workspace/src/components/workbench/layout/PreviewPanel.tsx#L168-L188) 每次切 tab 卸载 Canvas。

**方案**：保持 Plot3DScene 常驻 mount，用 CSS `display:none` 切换可见性；或用单例 WebGLRenderer + 多 scene。

```typescript
// Plot3DScene.tsx 改造
gl={{
  antialias: true,
  alpha: false,
  preserveDrawingBuffer: false,  // 改为 false
  powerPreference: 'high-performance',
}}
onCreated={({ gl }) => {
  glRef.current = gl;
}}
// 导出时手动渲染一帧
const exportPNG = () => {
  gl.render(scene, camera);
  return gl.domElement.toDataURL('image/png');
};
```

### 4.6 修复 5：采样进 Web Worker

**现状**：[plot3d.ts:162-188](file:///workspace/src/lib/plots/plot3d.ts#L162-L188) N=100 时 10000 次 evaluate 阻塞主线程。

**方案**：新建 `src/lib/plots/plot3d.worker.ts`：

```typescript
// worker 内
import { create, all } from 'mathjs';
const math = create(all);

self.onmessage = (e) => {
  const { expr, xRange, yRange, resolution } = e.data;
  const compiled = math.compile(expr);
  // ... 采样循环
  self.postMessage({ vertices, normals, colors, indices });
};
```

主线程 `Plot3DPanel` 用 `useWorker` hook 调用，UI 不卡。

### 4.7 修复 6：3D 轴考虑 Z 范围

**现状**：[Plot3DScene.tsx:379-393](file:///workspace/src/components/workbench/plots/Plot3DScene.tsx#L379-L393) `axisSize` 只取 X/Y 域。

**修复**：axisSize = `max(|xRange|, |yRange|, |zRange|) + margin`。

---

## 五、蓝图重建：解决"只会加减乘除"

### 5.1 根因定位

| 用户抱怨 | 根因代码 | 修复方向 |
|---|---|---|
| "加减乘除变来变去" | [pipelineEngine.ts:188-511](file:///workspace/src/components/workbench/nodes/pipelineEngine.ts#L188-L511) 仅 14 节点 | 扩展到 40+ |
| "没用" | [NodePipeline.tsx:237-238](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L237-L238) local state | 提升到全局 store |
| "改图后绘图不进预览" | [NodePipeline.tsx:353-395](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L353-L395) 无 setActivePreviewTab | Run 后切 tab |

### 5.2 修复 1：状态提升到全局 store

**现状**：[NodePipeline.tsx:237-238](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx#L237-L238)：
```tsx
const [nodes, setNodes] = useState<PipelineNode[]>(...);
const [edges, setEdges] = useState<PipelineEdge[]>(...);
```

**改造**（[workbench.ts](file:///workspace/src/lib/store/workbench.ts)）：

```typescript
interface WorkbenchState {
  // ... 现有
  pipeline: {
    nodes: PipelineNode[];
    edges: PipelineEdge[];
    selectedNodeIds: string[];
    past: { nodes: PipelineNode[]; edges: PipelineEdge[] }[];
    future: { nodes: PipelineNode[]; edges: PipelineEdge[] }[];
  };
  addNode / removeNode / connectNodes / disconnectNodes
  selectNodes / moveNodes
  undo / redo
}
```

**收益**：
- AI 可读改蓝图（"AI，把 derivative 连到 plot"）
- 命令面板可触发蓝图操作
- 撤销/重做天然实现
- 状态可序列化分享

### 5.3 修复 2：节点生态扩展到 40+

**现状 14 种** → **目标 40+**，分三层：

#### 输入层（8 种）
| 节点 | 输出 | 说明 |
|---|---|---|
| `sketch-input` | expression/geometry | **手绘画板**（第六节） |
| `csv-input` | matrix | 粘贴 CSV |
| `range-input` | array | `[a, a+step, ..., b]` |
| `linspace-input` | array | NumPy 风格 |
| `random-input` | number/array | 随机数 |
| `image-input` | image | 加载图片（第七节图像→曲线） |
| `table-input` | matrix | 表格 |
| `constant-input` | number | 常量（已有） |

#### 计算层（18 种）
| 节点 | 说明 |
|---|---|
| `statistics` | mean/std/var/median |
| `regression` | 线性/多项式拟合 + R² |
| `interpolation` | 拉格朗日/样条 |
| `fft` | 快速傅里叶 |
| `derivative-nth` | 高阶导 |
| `integrate-adaptive` | 自适应积分 |
| `matrix-decomp` | LU/QR/SVD/Cholesky |
| `eigen-analysis` | 特征值/向量/条件数 |
| `geometry-op` | CSG/变换（平移/旋转/缩放） |
| `data-transform` | map/filter/reduce |
| `convolution` | 卷积 |
| `differential-eq` | ODE 数值解 |
| `optimization` | 最小化 |
| `fourier-series` | 傅里叶级数 |
| `taylor-expand` | 泰勒（已有，需暴露） |
| `laplace` | 拉普拉斯变换 |
| `probability` | 概率分布（正态/泊松/二项） |
| `matrix-op` | 已有，扩展 |

#### 输出层（10 种）
| 节点 | 说明 |
|---|---|
| `plot-overlay` | 多曲线叠加 |
| `plot-3d-geometry` | 3D 几何体渲染 |
| `plot-3d-surface` | 3D 曲面（已有） |
| `table-output` | 数据表 |
| `latex-output` | LaTeX 片段 |
| `report-output` | Markdown 报告 |
| `export-csv` | CSV 文件 |
| `export-svg` | SVG 文件 |
| `export-gltf` | GLTF 3D 文件 |
| `display` | 显示（已有） |

### 5.4 修复 3：交互补全

| 改进 | 优先级 | 实现 |
|---|---|---|
| **双向连线** | P0 | input 端起拖也建边 |
| **撤销/重做** | P0 | past/future 栈 |
| **多选/框选** | P0 | selectedIds: Set + Shift 多选 |
| **右键菜单** | P0 | 空白→添加；节点→复制/删除/断开 |
| **环预防** | P1 | 连边瞬间拓扑检查 |
| **边端点重连** | P1 | 拖端点到新端口 |
| **端口类型色码** | P1 | number=teal/expression=amber/matrix=violet/geometry=rose |
| **Run 后切预览** | P0 | `setActivePreviewTab('plot2d')` |
| **单击边不删** | P0 | 改为选中+确认 |

### 5.5 修复 4：考虑迁移到 @xyflow/react

**现状**：[NodePipeline.tsx](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx) 纯手写 SVG + absolute div，无 react-flow。

**评估**：
- 手写已有 1700+ 行，迁移成本高
- 但 react-flow 自带：撤销/重做、多选、框选、右键菜单、minimap、边重连、环预防
- **建议**：长期迁移到 `@xyflow/react`，短期先补交互

---

## 六、白板：统一交互层（核心差异化）

### 6.1 用户核心诉求

> "白板既可单独存在，又可放进节点蓝图。蓝图可以把所有功能联系起来。"

**这是本文档最重要的主张**：把白板定位为**统一交互入口**，不是独立功能。

### 6.2 白板的三种模式

```
模式 1: 独立白板（全屏）
┌─────────────────────────────────┐
│  [手绘 Canvas 全屏]              │
│  工具栏: 画笔/橡皮/选择/识别     │
│  模式: [函数] [几何体] [图像]    │
│  [识别] → 弹出结果 → [发送到蓝图]│
└─────────────────────────────────┘

模式 2: 蓝图节点（sketch-input）
┌─────────────────────────────┐
│ ✏️ 手绘画板              [×] │
├─────────────────────────────┤
│ [手绘 Canvas 200×120]       │
│ 模式: [函数] [几何体]        │
│ [识别] [清除]               │
│ 结果: y = x² - 3            │
├─────────────────────────────┤
│ ○ expression  ○ geometry    │
└─────────────────────────────┘

模式 3: 2D/3D 触发器
  在 2D 绘图面板点"手绘"按钮 → 弹白板 → 识别 → 直接生成 plot
  在 3D 面板点"手绘"按钮 → 弹白板 → 识别几何体 → 直接生成 3D 模型
```

### 6.3 白板与蓝图的关系

```
白板（独立）──识别──→ 直接生成 plot（不经蓝图）
   │
   └──作为节点──→ 蓝图编排 ──→ 多步计算 ──→ 多种输出
```

**关键**：白板是"输入入口"，蓝图是"流程编排"。简单需求走白板直出，复杂需求走蓝图编排。

### 6.4 白板识别能力

| 输入 | 识别输出 | 技术 |
|---|---|---|
| 手写 `y=x²-3` | `{type:'function', latex:'y=x^2-3'}` | GPT-4o 视觉（复用 ai-client.ts） |
| 手绘圆/椭圆 | `{type:'geometry', kind:'circle', center, radius}` | GPT-4o 或轮廓检测 |
| 手绘立方体草图 | `{type:'geometry', kind:'cube', side}` | GPT-4o + 手动选类型 |
| 导入图片 | 提取轮廓 → 拟合曲线 | 见第七节 |
| 滑块动画截图 | 提取动点轨迹 | 见第七节 |

### 6.5 白板技术实现

**新增目录**：`src/components/whiteboard/`

```
whiteboard/
├── Whiteboard.tsx          # 独立白板主组件
├── WhiteboardCanvas.tsx    # 手绘 Canvas（pointer 事件采集笔迹）
├── WhiteboardToolbar.tsx   # 工具栏
├── RecognizePanel.tsx      # 识别结果面板
└── SketchNode.tsx          # 蓝图节点版（嵌入 NodePipeline）
```

**笔迹存储**：SVG path data（比 PNG 小 10 倍，可缩放）：

```typescript
interface Stroke {
  points: [number, number][];
  color: string;
  width: number;
}
// 序列化为 SVG path: M x0 y0 L x1 y1 L x2 y2 ...
```

**识别调用**（复用 [ai-client.ts](file:///workspace/src/lib/ai-client.ts)）：

```typescript
export async function recognizeSketch(
  strokes: Stroke[],
  mode: 'function' | 'geometry' | 'auto'
): Promise<RecognitionResult> {
  // strokes → PNG base64
  const png = strokesToPNG(strokes);
  // 调 GPT-4o 视觉
  return await chatComplete([{
    role: 'user',
    content: [
      { type: 'text', text: `识别这张手绘${mode}草图，返回 JSON` },
      { type: 'image_url', image_url: { url: `data:image/png;base64,${png}` } },
    ],
  }]);
}
```

---

## 七、图像→曲线反向功能（用户创意）

### 7.1 用户原话

> "滑块可以调整图像，然后我们可以根据它的图像轮廓，然后画出描绘这个图像的曲线。我觉得这很有意思。"

### 7.2 功能定义

**正向**（已有思路）：滑块驱动 plot → 生成动态图像
**反向**（用户创意）：从图像提取轮廓 → 拟合曲线 → 生成数学表达式

### 7.3 实现方案

#### 流程
```
1. 图像来源：上传图片 / 截图 / 滑块动画截图 / 白板手绘
2. 边缘检测：Sobel/Canny 算法（纯 JS，无依赖）
3. 轮廓提取：marching squares 或轮廓追踪
4. 点集采样：从轮廓采样 N 个 (x, y) 点
5. 曲线拟合：多项式回归 / 样条插值
6. 输出：数学表达式 + plot 叠加对比
```

#### 关键代码

```typescript
// src/lib/image/contourExtractor.ts
export function extractContour(
  imageData: ImageData,
  threshold: number = 128
): [number, number][] {
  // 1. 灰度化
  // 2. Sobel 边缘检测
  // 3. 轮廓追踪（Moore 邻域算法）
  // 返回轮廓点集
}

// src/lib/image/curveFitter.ts
export function fitCurve(
  points: [number, number][],
  degree: number = 3
): { expression: string; rSquared: number } {
  // 最小二乘多项式拟合
  // 返回 mathjs 表达式 + R²
}
```

#### 蓝图节点

```
[image-input] → [contour-extract] → [curve-fit] → [plot-output]
                                          ↓
                                   [display: y = 0.3x³ - 1.2x² + 2x - 0.5]
```

### 7.4 滑块动画捕捉（进阶）

```
1. 用户设滑块 a 从 0→10，步长 0.5
2. 每步截图 plot
3. 提取每帧的曲线
4. 生成"曲线族"动画
5. 或提取某点的轨迹（如极值点随 a 变化的轨迹）
```

这是 Desmos 都没有的功能，**强差异化**。

---

## 八、GitHub 资源整合方案

### 8.1 调研结果

| 库 | License | 用途 | 推荐度 |
|---|---|---|---|
| [JSXGraph](https://github.com/jsxgraph/jsxgraph) | MIT/LGPL | 2D 交互几何 + 函数绘图 + 滑块 + 隐函数 | ⭐⭐⭐⭐⭐ 最接近 GeoGebra |
| [react-math-viz](https://www.npmjs.com/package/react-math-viz) | - | React 2D SVG + 3D ThreeJS 组件库 | ⭐⭐⭐⭐ 已实现隐函数/参数/向量场/热力图/极坐标 |
| [function-plot](https://github.com/mauriciopoppe/function-plot) | MIT | 轻量 D3 函数绘图 | ⭐⭐⭐ 备选 |
| [pajamadot/geometry-node](https://github.com/pajamadot/geometry-node) | - | Blender 风格几何节点系统 30+ 节点 | ⭐⭐⭐⭐ 蓝图参考 |
| [lewisgoing/threejs-visual-editor](https://github.com/lewisgoing/threejs-visual-editor) | - | R3F 节点编辑器 | ⭐⭐⭐ 3D 节点参考 |
| [mathsteps](https://github.com/socraticorg/mathsteps) | MIT | 步骤求解（教学） | ⭐⭐⭐⭐ 数学教育 |
| [mathjs-simple-integral](https://github.com/joelhoover/mathjs-simple-integral) | MIT | mathjs 积分扩展 | ⭐⭐⭐⭐ 补全积分 |
| [Algebrite](https://github.com/davidedc/Algebrite) | MIT | CAS 符号计算 | ⭐⭐⭐ 备选 CAS |
| [Nerdamer](https://github.com/MenloSystems/nerdamer) | MIT | 符号数学 | ⭐⭐⭐ 备选 |
| [Cortex Compute Engine](https://github.com/cortex-js/compute-engine) | MIT | LaTeX↔MathJSON | ⭐⭐⭐⭐ LaTeX 解析 |
| [@xyflow/react](https://github.com/xyflow/xyflow) | MIT | React Flow 节点编辑器 | ⭐⭐⭐⭐⭐ 蓝图基础 |

### 8.2 整合建议

#### 2D 绘图：评估 react-math-viz 替换 Plot2DCanvas

**现状**：[Plot2DCanvas.tsx](file:///workspace/src/components/workbench/plots/Plot2DCanvas.tsx) 1700+ 行手写，有 D1-D7 共 7 个问题。

**react-math-viz 已实现**：
- `Function2D` / `Parametric2D` / `Implicit2D` / `PolarFunction2D`
- `VectorField2D` / `Heatmap2D` / `Contour2D`
- `Crosshair2D` / `Legend2D` / `Label`
- `Area2D`（积分面积）/ `AngleMarker` / `DistanceMarker`
- SVG 渲染 + HTML overlay 标签

**建议**：
- **短期**：修复 Plot2DCanvas 的 D1-D7（止血）
- **中期**：用 react-math-viz 重写 2D 层，省 1700 行代码 + 获得 15+ 组件
- **长期**：或评估 JSXGraph（更接近 GeoGebra，支持可拖点）

#### 蓝图：迁移到 @xyflow/react

**现状**：[NodePipeline.tsx](file:///workspace/src/components/workbench/nodes/NodePipeline.tsx) 1700+ 行手写，缺 P3 所有交互。

**@xyflow/react 自带**：撤销/重做、多选、框选、右键菜单、minimap、边重连、环预防、自定义节点。

**建议**：
- **短期**：补交互（P0 项）
- **中期**：迁移到 @xyflow/react，省大量代码 + 获得专业交互

#### 数学引擎：扩展 mathjs

**现状**：[evaluator.ts](file:///workspace/src/lib/engine/evaluator.ts) 用 mathjs 但积分是数值（Simpson）。

**建议**：
- 引入 `mathjs-simple-integral`：符号积分
- 引入 `mathsteps`：步骤求解（教学场景）
- 引入 `Algebrite` 或 `Nerdamer`：更强的 CAS（备选）

#### 3D：保留自写 + 加 three-bvh-csg

**现状**：[Plot3DScene.tsx](file:///workspace/src/components/workbench/plots/Plot3DScene.tsx) 用 R3F，基础 OK。

**建议**：
- 保留自写（react-math-viz 的 3D 较弱）
- 加 `three-bvh-csg`：截面（CSG 布尔运算）
- 加 `troika-three-text`：3D 文本标注（drei 已含）

---

## 九、收敛的执行路线

> 用户说"全部定义起来太乱"。以下是**有主张的优先级**，不是功能清单。

### 9.1 第一优先：引擎统一（解决 E1-E3）

**为什么第一**：4 个 mathjs 实例 scope 不共享是所有"行为不一致"的根因。不修这个，2D/蓝图改了也白搭。

**任务**：
1. 新建 `src/lib/engine/mathInstance.ts`：单例 mathjs + 共享 scope + 统一 log/ln 重写
2. evaluator.ts / plot2d.ts / plot3d.ts / pipelineEngine.ts 全部改为 import 单例
3. 删除 4 处 `create(all)`

**验证**：editor 写 `a=5` → plot `sin(a*x)` 能画出图。

### 9.2 第二优先：2D 滑块激活（解决 D2）

**为什么第二**：这是 Desmos 80% 体验来源，成本最低，立竿见影。

**任务**：
1. Plot2DCanvas `computed` 加 `variables` 依赖
2. VariablesPanel 滑块 onChange → setVariable → 触发重算
3. 加 SliderRow 的 ▶ 动画按钮

**验证**：输入 `y=ax²` → 自动出 a 滑块 → 拖动曲线变。

### 9.3 第三优先：Y 轴双轴（解决 D1）

**为什么第三**：直接解决"sin 和 e^x 比例不一样"。

**任务**：
1. Plot2DPanel 加 Y 轴模式切换（shared/dual/normalized）
2. Plot2DCanvas 支持副轴绘制
3. 图层面板加轴归属

**验证**：sin + e^x 同图，dual 模式下两者都清晰可见。

### 9.4 第四优先：白板 MVP（差异化王牌）

**为什么第四**：这是 AI 黑板 + Desmos 都没有的融合点。

**任务**：
1. 新建 `src/components/whiteboard/` 目录
2. WhiteboardCanvas（手绘 + 笔迹采集）
3. 接 GPT-4o 视觉识别（复用 ai-client.ts）
4. 独立白板模式 + sketch-input 蓝图节点

**验证**：白板手写 `y=x²` → 识别 → 生成 plot。

### 9.5 第五优先：3D 几何体引擎（解决 T1）

**为什么第五**：解决"只看个图"。

**任务**：
1. types.ts 加 GeometryShape
2. evaluator.ts 加 cube/sphere/cone 等动词
3. Plot3DScene 加 GeometryMesh
4. 加 three-bvh-csg 截面

**验证**：输入 `cube(3)` → 3D 立方体 → 切截面 → 2D 视图。

### 9.6 第六优先：蓝图状态提升（解决 P1-P2）

**为什么第六**：让蓝图"活起来"，能被 AI/命令面板访问。

**任务**：
1. workbench.ts 加 pipeline state
2. NodePipeline 改用 store
3. Run 后 setActivePreviewTab
4. 补撤销/重做/右键

**验证**：AI 能读蓝图状态；Run 后自动切预览。

### 9.7 第七优先：节点扩展到 30+

**为什么第七**：让蓝图"有用"。

**任务**：按 5.3 节扩展输入/计算/输出层节点。

### 9.8 第八优先：图像→曲线反向（用户创意）

**为什么第八**：强差异化，但依赖前面的基础。

**任务**：按第七节实现 contourExtractor + curveFitter + 蓝图节点。

### 9.9 不要做的事

- ❌ 不要逐个抄 Desmos 功能（抓范式，不抄功能）
- ❌ 不要先做插件系统（先验证核心）
- ❌ 不要做虚拟人/课堂实录（非核心）
- ❌ 不要全量替换 Plot2DCanvas（先修，再评估 react-math-viz）
- ❌ 不要急着迁移 react-flow（先补交互，再迁移）

---

## 附：关键资源链接

- [JSXGraph](https://github.com/jsxgraph/jsxgraph) — 最接近 GeoGebra 的开源库
- [react-math-viz](https://www.npmjs.com/package/react-math-viz) — React 数学可视化组件库
- [@xyflow/react](https://github.com/xyflow/xyflow) — React Flow 节点编辑器
- [pajamadot/geometry-node](https://github.com/pajamadot/geometry-node) — 几何节点系统参考
- [mathjs extensions](https://mathjs.org/download.html) — mathjs-simple-integral / mathsteps / Algebrite
- [three-bvh-csg](https://github.com/gkjohnson/three-bvh-csg) — Three.js CSG 布尔运算
- [troika-three-text](https://github.com/protectwise/troika) — 3D 文本

---

*本文档基于 2026-07-24 完整代码审查（15 文件实读）+ GitHub 资源调研 + 用户核心诉求收敛生成。*
